# TritonAI Developer API (LiteLLM, OpenAI-compatible). Server-side only.
# Get your key from https://tritonai-api.ucsd.edu/
TRITONAI_API_KEY=
TRITONAI_BASE_URL=https://tritonai-api.ucsd.edu/v1
TRITONAI_MODEL=api-gemma-4-31b
# Tagging only — a short classification pass, retried up to 3 times.
AI_TIMEOUT_MS=25000
# Every reasoning-tier call: drafting, clustering, gap analysis, the brief, model-to-map,
# and baseline synthesis. These send whole transcripts or the whole reference library and
# run for tens of seconds. One attempt, kept under the routes' maxDuration (60s) so the
# request always answers and logs. Raise this — not AI_TIMEOUT_MS — if drafts time out.
AI_TIMEOUT_MS_REASONING=50000
# Optional: override the reasoning budget for baseline synthesis alone (the longest call).
# AI_TIMEOUT_MS_SYNTHESIS=50000

# Optional per-feature model tiers. Both fall back to TRITONAI_MODEL when unset.
# TRITONAI_MODEL_FAST       — tagging (verbatim-grounded classification; cheap is fine).
# TRITONAI_MODEL_REASONING  — journey/friction drafting and friction clustering
#                             (synthesis + root-cause reasoning; a more capable model
#                             supports auto-applying these without per-item review).
TRITONAI_MODEL_FAST=
TRITONAI_MODEL_REASONING=

# Optional embeddings model for the Reference Library's semantic retrieval. When unset,
# the library index and gap analysis fall back to keyword (lexical) retrieval — the feature
# works either way, embeddings just improve passage matching.
TRITONAI_MODEL_EMBED=

# GitHub-as-storage. Fine-grained PAT scoped to THIS repo, Contents: read/write.
GITHUB_TOKEN=
GITHUB_REPO=z6johnson/ai-transformation
GITHUB_BRANCH=data

# Identity stamped on saved artifacts and the AI decision log.
PRACTICE_ACTOR=z6johnson@ucsd.edu
